/** Legacy /tenant/dashboard shell — pages redirect to /console/dashboard. */
export default function LegacyTenantDashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
