NEXT_PUBLIC_GATEWAY_URL=http://localhost:8000

# Local tenant login on localhost — must match tenant `domain` in super-admin UI
# (no hosts file / DNS needed; gateway reads X-Tenant-Domain in development)
NEXT_PUBLIC_TENANT_DEV_DOMAIN=labs.aaptor.com
