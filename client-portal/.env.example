# API Gateway URL (cloud-gateway)
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8000

# Manage-users portal uses /api/manage on the gateway (proxied to cloud_automation).
# Organization admin portal uses /api/org-admin on the gateway (proxied to cloud_automation).
