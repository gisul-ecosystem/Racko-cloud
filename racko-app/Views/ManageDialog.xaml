<Window x:Class="RackoApp.Views.ManageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Shared File"
        Width="460" Height="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource SurfaceBrush}">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>  <!-- Title -->
      <RowDefinition Height="Auto"/>  <!-- File name (read-only) -->
      <RowDefinition Height="Auto"/>  <!-- Permission -->
      <RowDefinition Height="*"/>     <!-- VM list -->
      <RowDefinition Height="Auto"/>  <!-- Buttons -->
    </Grid.RowDefinitions>

    <!-- Title -->
    <TextBlock Grid.Row="0" x:Name="TitleText"
               FontSize="18" FontWeight="Bold"
               Foreground="{StaticResource TextPrimary}" Margin="0,0,0,20"
               TextTrimming="CharacterEllipsis"/>

    <!-- File info (read-only) -->
    <StackPanel Grid.Row="1" Margin="0,0,0,16">
      <TextBlock Text="File" FontSize="13" FontWeight="SemiBold"
                 Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
      <Border Background="#F8FAFC" BorderBrush="{StaticResource BorderBrush}"
              BorderThickness="1" CornerRadius="8" Padding="10,8">
        <TextBlock x:Name="FileInfoText" FontSize="13"
                   Foreground="{StaticResource TextSecondary}"
                   TextTrimming="CharacterEllipsis"/>
      </Border>
    </StackPanel>

    <!-- Permission -->
    <StackPanel Grid.Row="2" Margin="0,0,0,16">
      <TextBlock Text="Permission" FontSize="13" FontWeight="SemiBold"
                 Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
      <StackPanel Orientation="Horizontal">
        <RadioButton x:Name="PermRead"      Content="Read Only"        GroupName="mperm" Margin="0,0,16,0" FontSize="13"/>
        <RadioButton x:Name="PermReadWrite" Content="Read &amp; Write" GroupName="mperm" Margin="0,0,16,0" FontSize="13"/>
        <RadioButton x:Name="PermFull"      Content="Full Control"     GroupName="mperm" FontSize="13"/>
      </StackPanel>
    </StackPanel>

    <!-- VM list -->
    <StackPanel Grid.Row="3" Margin="0,0,0,16">
      <TextBlock Text="Share with VMs" FontSize="13" FontWeight="SemiBold"
                 Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
      <Border Background="White" BorderBrush="{StaticResource BorderBrush}"
              BorderThickness="1" CornerRadius="8" MaxHeight="160">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <ItemsControl x:Name="VmList" Margin="8">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <CheckBox Content="{Binding Name}" Tag="{Binding Id}"
                          Margin="4,6" FontSize="13" Cursor="Hand"/>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </Border>
    </StackPanel>

    <!-- Buttons: Delete on left, Cancel + Save on right -->
    <Grid Grid.Row="4">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <Button Grid.Column="0" Content="Delete File"
              Style="{StaticResource DangerButton}"
              HorizontalAlignment="Left"
              Click="OnDeleteClick"/>

      <StackPanel Grid.Column="1" Orientation="Horizontal">
        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="OnCancelClick"/>
        <Button Content="Save Changes" Style="{StaticResource PrimaryButton}" Click="OnSaveClick"/>
      </StackPanel>
    </Grid>
  </Grid>
</Window>
