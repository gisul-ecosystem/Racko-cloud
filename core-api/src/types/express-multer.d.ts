/**
 * Ensures Express.Request includes multer's `file` / `files` when @types/multer is installed.
 * Run `npm install` (include devDependencies) so @types/multer is present.
 */
/// <reference types="multer" />
