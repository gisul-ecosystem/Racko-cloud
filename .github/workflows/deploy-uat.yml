# GitHub secrets (Settings → Secrets and variables → Actions):
#   DOCKERHUB_USERNAME   — Docker Hub username
#   DOCKERHUB_TOKEN    — Docker Hub access token (read/write)
#   VM_HOST_UAT        — VM public IP or hostname (e.g. 103.99.38.159)
#   VM_USER_UAT        — SSH user (e.g. ubuntu)
#   VM_SSH_KEY_UAT     — Private SSH key for the VM user
#
# Service env files live on the VM under /opt/racko-uat/<service>/.env (e.g. cloud-automation/.env) — not in GitHub secrets.
# client-portal image bakes NEXT_PUBLIC_GATEWAY_URL=http://cloud-gateway:8000 (Docker internal).
# Nginx must proxy /api/ → :8000 on uat.racko.ai and each tenant domain.

name: Build & Deploy UAT via SSH

on:
  push:
    branches: [uat]
  workflow_dispatch:

env:
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build-core-api:
    name: Build & Push core-api
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker login
        run: echo "${{ env.DOCKERHUB_TOKEN }}" | docker login -u "${{ env.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build core-api image
        run: docker build -t ${{ env.DOCKERHUB_USERNAME }}/racko-core-api:${{ env.IMAGE_TAG }} -t ${{ env.DOCKERHUB_USERNAME }}/racko-core-api:uat -f core-api/Dockerfile core-api/

      - name: Push core-api image
        run: |
          docker push ${{ env.DOCKERHUB_USERNAME }}/racko-core-api:${{ env.IMAGE_TAG }}
          docker push ${{ env.DOCKERHUB_USERNAME }}/racko-core-api:uat

  build-cloud-gateway:
    name: Build & Push cloud-gateway
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker login
        run: echo "${{ env.DOCKERHUB_TOKEN }}" | docker login -u "${{ env.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build cloud-gateway image
        run: docker build -t ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-gateway:${{ env.IMAGE_TAG }} -t ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-gateway:uat -f cloud-gateway/Dockerfile cloud-gateway/

      - name: Push cloud-gateway image
        run: |
          docker push ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-gateway:${{ env.IMAGE_TAG }}
          docker push ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-gateway:uat

  build-cloud-automation:
    name: Build & Push cloud-automation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker login
        run: echo "${{ env.DOCKERHUB_TOKEN }}" | docker login -u "${{ env.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build cloud-automation image
        run: docker build -t ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-automation:${{ env.IMAGE_TAG }} -t ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-automation:uat -f cloud_automation/Dockerfile cloud_automation/

      - name: Push cloud-automation image
        run: |
          docker push ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-automation:${{ env.IMAGE_TAG }}
          docker push ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-automation:uat

  build-cloud-automation-aws:
    name: Build & Push cloud-automation-aws
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker login
        run: echo "${{ env.DOCKERHUB_TOKEN }}" | docker login -u "${{ env.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build cloud-automation-aws image
        run: docker build -t ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-automation-aws:${{ env.IMAGE_TAG }} -t ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-automation-aws:uat -f cloud_automation_aws/Dockerfile cloud_automation_aws/

      - name: Push cloud-automation-aws image
        run: |
          docker push ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-automation-aws:${{ env.IMAGE_TAG }}
          docker push ${{ env.DOCKERHUB_USERNAME }}/racko-cloud-automation-aws:uat

  build-client-portal:
    name: Build & Push client-portal
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker login
        run: echo "${{ env.DOCKERHUB_TOKEN }}" | docker login -u "${{ env.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build client-portal image
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_GATEWAY_URL=https://api-uat.racko.ai \
            -t ${{ env.DOCKERHUB_USERNAME }}/racko-client-portal:${{ env.IMAGE_TAG }} \
            -t ${{ env.DOCKERHUB_USERNAME }}/racko-client-portal:uat \
            -f client-portal/Dockerfile client-portal/

      - name: Push client-portal image
        run: |
          docker push ${{ env.DOCKERHUB_USERNAME }}/racko-client-portal:${{ env.IMAGE_TAG }}
          docker push ${{ env.DOCKERHUB_USERNAME }}/racko-client-portal:uat

  deploy:
    name: Deploy to UAT VM
    runs-on: ubuntu-latest
    needs:
      - build-core-api
      - build-cloud-gateway
      - build-cloud-automation
      - build-cloud-automation-aws
      - build-client-portal

    env:
      VM_HOST: ${{ secrets.VM_HOST_UAT }}
      VM_USER: ${{ secrets.VM_USER_UAT }}
      VM_SSH_KEY: ${{ secrets.VM_SSH_KEY_UAT }}
      DEPLOY_PATH: /opt/racko

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${VM_SSH_KEY}" > ~/.ssh/vm_key
          chmod 600 ~/.ssh/vm_key
          ssh-keyscan -H "${VM_HOST}" >> ~/.ssh/known_hosts 2>/dev/null

      - name: Upload files to VM
        run: |
          ssh -i ~/.ssh/vm_key -o StrictHostKeyChecking=no "${VM_USER}@${VM_HOST}" "mkdir -p ${DEPLOY_PATH}"
          scp -i ~/.ssh/vm_key -o StrictHostKeyChecking=no docker-compose-uat.yml "${VM_USER}@${VM_HOST}:${DEPLOY_PATH}/docker-compose.yml"
          echo "docker-compose-uat.yml uploaded to ${DEPLOY_PATH}"

      - name: Deploy on VM (pull new images + restart)
        run: |
          ssh -i ~/.ssh/vm_key -o StrictHostKeyChecking=no "${VM_USER}@${VM_HOST}" \
            "set -e; \
             echo 'Cleaning up Docker system...'; \
             sudo docker system prune -a --volumes -f; \
             sudo docker container prune -f; \
             sudo docker image prune -a -f; \
             sudo docker volume prune -f; \
             echo 'Cleaning Docker logs...'; \
             sudo find /var/lib/docker/containers -name '*.log' -delete; \
             echo 'Cleaning journal logs...'; \
             sudo journalctl --vacuum-size=100M; \
             echo 'Cleaning apt cache...'; \
             sudo apt clean; \
             sudo apt autoclean; \
             echo 'Disk usage after cleanup:'; \
             df -h; \
             cd ${DEPLOY_PATH}; \
             export DOCKERHUB_USERNAME='${DOCKERHUB_USERNAME}'; \
             export IMAGE_TAG='uat'; \
             echo 'Docker login...'; \
             echo '${DOCKERHUB_TOKEN}' | docker login -u '${DOCKERHUB_USERNAME}' --password-stdin; \
             echo 'Deploying...'; \
             sudo docker compose -f docker-compose.yml down --remove-orphans; \
             sudo docker compose -f docker-compose.yml pull; \
             sudo docker compose -f docker-compose.yml up -d; \
             sudo docker compose -f docker-compose.yml ps"

      - name: Summary
        run: |
          echo "Build & Deploy UAT Complete!"
          echo "Client portal:  http://${VM_HOST}:3000"
          echo "Cloud gateway:  http://${VM_HOST}:8000"
          echo "Core API:       http://${VM_HOST}:8001"
          echo "Azure service:  http://${VM_HOST}:3002"
          echo "AWS service:    http://${VM_HOST}:3003"
